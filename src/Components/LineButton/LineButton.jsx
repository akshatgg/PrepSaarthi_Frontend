import React from 'react';
import './LineButton.css';

const LineButton = () => {
  return (
    <section className="line-buttons">
     
      <div className="line-container">
        {/* Button 2 */}
        <a className="line-btn line-btn-2">Join Us</a>
      </div>
    </section>
  );
};

export default LineButton;
